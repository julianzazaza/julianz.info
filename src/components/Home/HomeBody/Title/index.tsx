import React from 'react';
import {
    StyledTitle
} from './index.style';

const Title = () => {

    return (
        <StyledTitle>
            I have a web site now
        </StyledTitle>
    )
}

export default Title;